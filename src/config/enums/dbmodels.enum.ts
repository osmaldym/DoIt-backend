export enum DBModel {
    user = 'User',
    task = 'Task',
    category ='Category',
}

export enum MagicStrings {
    USER = 'USER_MODEL',
    TASK = 'TASK_MODEL',
    CATEGORY = 'CATEGORY_MODEL',
}